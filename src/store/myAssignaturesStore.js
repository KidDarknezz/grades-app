const state = {};

const mutations = {};

const actions = {
  getUserInfoAndAssignatures({}, payload) {
    console.log("getting info");
  },
};

const getters = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
};
